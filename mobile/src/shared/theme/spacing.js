/** Rootaroo spacing + radius tokens — from 01-Design-System.html. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  huge: 48,
  massive: 80,
};

export const radius = {
  xs: 4,
  sm: 8,
  md: 12,
  // Card corner radius — the most common value in actual use for card/row
  // surfaces, sitting between `md` and `lg`.
  card: 14,
  lg: 16,
  // Large card / panel corner radius (feed media, bigger tiles).
  cardLg: 18,
  xl: 20,
  // Bottom-sheet / modal corner radius.
  sheet: 24,
  pill: 9999,
};

export const layout = {
  screenPadding: 20,
  contentMaxWidth: 560,
  bottomNavHeight: 64,
};
